{{- if .Values.skupper.enabled }}
{{- range .Values.skupper.services }}
---
apiVersion: skupper.io/v2alpha1
kind: Listener
metadata:
  name: {{ .name }}-remote
spec:
  routingKey: {{ .name }}
  host: {{ .name }}-remote
  port: {{ .port }}
{{- end }}
{{- end }}
